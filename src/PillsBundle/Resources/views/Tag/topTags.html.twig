<h4>Top Tag</h4>
<ul>
    {% for tag in tags %}
        <li>
            <a href="{{ path('get_posts_by_tag', {'tag': tag.hashSlug}) }}">{{ tag.hashTag|hashTag }}</a>
        </li>
    {% endfor %}
</ul>